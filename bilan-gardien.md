# Bilan Lab Git — Le Gardien

## 3 choses apprises dans les Labs Git

1. J'ai appris à créer des branches avec `git switch -c` pour travailler en parallèle sans perturber main.
2. J'ai compris comment un conflit Git survient quand deux personnes modifient la même ligne, et comment lire les marqueurs `<<<<<<<`, `=======`, `>>>>>>>`.
3. J'ai maîtrisé le workflow complet : branche → commit → push → Pull Request → review → merge → tag.
